{
  "name": "kcss",
  "version": "1.0.0",
  "description": "A simple flex based css framework",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build": "node-sass ./kCss.scss ./build/css/kCss.min.css --output-style compressed --source-map true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.9.2"
  }
}
